# Direct dependencies only; pip resolves the rest.
Flask==3.1.3          # serves the API and the static frontend
requests==2.34.2      # TMDB calls in database/build_catalog.py
python-dotenv==1.2.3  # loads TMDB credentials from .env

# Embedding the catalog. sentence-transformers pulls in torch (~2 GB installed),
# by far the heaviest thing here — it runs the model locally, no API key needed.
sentence-transformers==6.0.0
numpy==2.5.2
